---
- name: Ping Windows Server
  hosts: windows
  gather_facts: false
  tasks:
    - name: Ping the windows server
      ansible.windows.win_ping:
    - name: Open Chrome browser
      win_command: "C:\Program Files (x86)\Google\Chrome\Application\chrome.exe"
        # creates: "C: msedge.exe"

