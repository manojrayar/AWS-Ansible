---
- name: Ping Windows Server
  hosts: windows
  tasks:
    - name: Ping the windows server
      ansible.windows.win_ping:
    - name: Open Chrome browser
      win_command:
        cmd: start msedge.exe
        # creates: "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe"

